import { Component } from '@angular/core';

@Component({
  selector: 'app-dashboard',
  template: '<p style="color:var(--tx-md);padding:24px">Dashboard — próximamente</p>',
})
export class DashboardComponent {}
